{
  "name": "webai-chatbot",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync -w . --server --files 'sdk/**' --port 3000",
    "dev": "browser-sync -w . --server --files 'sdk/**' --port 3000",
    "commit": "git pull && bun update && read -p 'Descriptions Commit: ' message && git add . && echo -n \"$message - \" && node getTime.js | xargs -I {} git commit -m \"$message - {}\" && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}